# digitalthoughts.github.io
$ git checkout master
$ git branch new-branch
$ git checkout new-branch

# ...develop some code...

$ git add -A
$ git commit -m "Some commit message"
$ git checkout master
$ git merge new-branch


###########
$ git pull
$ git checkout -b test_code
$ git branch
$ git add -A
$ git commit -m "Some commit message"
$ git push origin test_code
$ git checkout master
$ git push origin master
$ git status

